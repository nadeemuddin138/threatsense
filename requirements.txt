# ThreatSense — pinned dependencies
# Versions verified against current PyPI releases (June 2026).
# Tip: after your first successful `pip install -r requirements.txt`,
# regenerate exact pins for your machine with:  pip freeze > requirements.txt

# --- Data & ML ---
pandas==3.0.2
numpy==2.4.4
scipy==1.17.1
scikit-learn==1.8.0
xgboost==3.2.0
imbalanced-learn==0.14.2      # SMOTE / sampling for class imbalance (Phase 2)
shap==0.52.0                  # model explainability
joblib==1.5.3                 # model + scaler serialization

# --- LLM agent (LangGraph + OpenAI GPT-4o) ---
langgraph==1.2.5
langchain-openai==1.3.2
openai==2.41.1

# --- Backend ---
fastapi==0.136.3
uvicorn==0.49.0
pydantic==2.13.4
python-multipart==0.0.32      # required for FastAPI file uploads

# --- Frontend ---
streamlit==1.58.0

# --- Plotting (confusion matrix, SHAP, dashboard) ---
matplotlib==3.11.0
seaborn==0.13.2

# --- Config ---
python-dotenv==1.2.2          # load OPENAI_API_KEY from .env

# --- Dev / CI ---
pytest==9.1.0
httpx==0.28.1                 # FastAPI TestClient dependency
ruff==0.15.17                 # linting in GitHub Actions
